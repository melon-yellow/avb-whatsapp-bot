
##########################################################################################################################

version: "3"

services:

##########################################################################################################################

  db:
    image: melon-yellow/mysql-8-avb-whatsapp-db
    container_name: avb-whatsapp-db
    build: ./db
    command: --default-authentication-plugin=mysql_native_password
    env_file: .env
    volumes:
      - ./db/data/:/var/lib/mysql
    ports:
      - "${MYSQL_PORT}:${MYSQL_PORT}"
    restart: always

##########################################################################################################################

  whatsapp:
    image: melon-yellow/node-17-avb-whatsapp
    container_name: avb-whatsapp
    build: ./whatsapp
    command: bash -c "yarn && yarn build && yarn nonstop"
    env_file: .env
    volumes:
      - ./whatsapp/:/usr/app/
      - /usr/app/build/
    ports:
      - "${WHATSAPP_PORT}:${WHATSAPP_PORT}"
    depends_on:
      - db
    restart: always

##########################################################################################################################
